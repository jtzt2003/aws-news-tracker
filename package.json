{
  "name": "aws-reinvent-tracker-vercel",
  "version": "1.0.0",
  "description": "AWS re:Invent announcement tracker - Vercel serverless deployment",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "deploy-preview": "vercel"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
